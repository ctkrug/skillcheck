---
name: Broken_Skill
description: does stuff
---

# Broken Skill

Three problems Skillcheck catches here:
- `name` is not kebab-case (`Broken_Skill`) — an error.
- `name` does not match its directory (`broken-skill`) — a warning.
- `description` has no trigger language — a warning.
